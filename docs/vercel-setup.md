# GitHub, Vercel, and staging-domain setup

Verified September 9, 2026. The Git connection and hosted Preview now work. The remaining owner actions below were not performed by this task. The live Wix website and production DNS remain unchanged.

## Verified project

- GitHub: SmokinJoeSJ/project-starburst; production branch main remains at e428a2ea5062998168c160168d03051052c98566.
- Vercel team: platinum-luxe-media; project: project-starburst; ID: prj_XXEwchChqgozS8PcrZBmxLR6CmQ3.
- The owner connected and bootstrapped this project before verification. Do not repeat the main deployment or re-import the repository.
- Git pushes created Preview deployments for redesign-2026 and feature/vercel-rsc-routing. The Vercel status and preview link appeared on [PR #2](https://github.com/SmokinJoeSJ/project-starburst/pull/2).
- Stable branch alias: https://project-starburst-git-redesign-2026-platinum-luxe-media.vercel.app. It follows redesign-2026; the custom staging domain is still pending.
- Existing domain assignments are project-starburst.plm.studio (no branch binding) and project-starburst.vercel.app (redirecting to that PLM domain). These owner-managed assignments were preserved. Do not use them as the redesign staging URL without separately reviewing their environment assignment.
- Vercel deployment protection is enabled for deployment URLs; authenticated checks used the Vercel CLI. Reviewers need authorized Vercel access or an owner-created share link. Protection currently excludes custom domains.
- No custom application environment variables were configured. System environment variables are exposed to builds.
- Local .vercel/project.json and .env.local were generated by CLI linking and remain ignored.
- See [hosted verification](staging-verification.md) for checks and the routing fix.

## Build settings

The deployed branch's vercel.json overrides the dashboard defaults:

| Setting | Actual Preview setting |
| --- | --- |
| Root Directory | Repository root |
| Framework | null / Other; not standard Next.js |
| Install Command | npm ci |
| Build Command | npm run build:vercel |
| Output Directory | dist/client |
| Vercel Node.js Version | 24.x; hosted build passed |
| GitHub Actions / local Node.js | 22.x; both build targets passed |
| Production Branch | main |
| Automatic branch allowlist | redesign-2026 and feature/** |

The dashboard's baseline preset is Vite, with command/output overrides unset. The branch configuration was honored; no dashboard build change is required for the working Preview. Aligning the dashboard preset with Other and Node with the documented 22.x is optional owner maintenance, not a reason to deploy main.

Preserve .openai/hosting.json and npm run build for the original Cloudflare/Sites target. Vercel uses npm run build:vercel only.

## Owner action: production and GitHub protections

The staging configuration disables main and unspecified branches and enables redesign-2026 and feature/**. However, **old main does not contain vercel.json**, so that file cannot block deployments from old main. No dashboard Ignored Build Step was configured at inspection. Keep main untouched.

1. In Vercel project Settings → Build and Deployment → Ignored Build Step, select **Only build preview** and save. Keep Production Branch main. Do not test this by deploying main. This dashboard guard is separate from the branch-local allowlist. [Ignored Build Step behavior](https://vercel.com/docs/project-configuration/project-settings)
2. In GitHub Settings → Rules → Rulesets, protect main and redesign-2026: require PRs, block force pushes/deletion, require resolved conversations, and require the observed **Validate site** and **Vercel** checks for staging PRs.
3. Require another authorized review where practical, dismiss stale approvals, and limit bypass privileges. GitHub does not allow an author to approve their own PR. If Joe is the only reviewer, define an owner review policy instead.
4. Continue creating feature/* PRs with explicit base redesign-2026. Only a future approved launch PR targets main. No rulesets were present at inspection.
5. Keep PLM_PRODUCTION_LAUNCH_APPROVED unset. Neither a green check nor ordinary staging review authorizes launch.

The Git allowlist was verified by actual feature and integration Preview deployments. It does not replace account permissions. [Vercel Git configuration](https://vercel.com/docs/project-configuration/git-configuration)

## Preview environment variables

No owner-provided variable is required to render the current staging website.

In Settings → Environment Variables, use **Preview only** if these optional behaviors are wanted:

- PLM_PREVIEW_EMAIL: dedicated public test inbox. Unset means an on-page draft; setting it opens a test email draft, not automatic delivery.
- PLM_STRIPE_TEST_PAYMENT_LINK: public Stripe test/sandbox Payment Link beginning with https://buy.stripe.com/test_. Unset means donations stay inactive.

Both can be visible in public static output. No secret keys are needed. Rebuild the Preview after changing them. VERCEL_ENV and VERCEL_GIT_COMMIT_REF are provider-managed; PLM_BUILD_TARGET is internal. Do not copy the local VERCEL_OIDC_TOKEN into project settings. See [environment safety](environment-safety.md).

## Owner action: one persistent custom staging domain

Use **preview.projectstarburst.org**.

1. In platinum-luxe-media / project-starburst → Settings → Domains, add preview.projectstarburst.org.
2. Edit its assignment: environment **Preview**, Git branch **redesign-2026**. Confirm the saved branch binding. Do not attach the apex or www.
3. The authoritative nameservers observed are **ns12.wixdns.net** and **ns13.wixdns.net**. In the Wix account managing projectstarburst.org, open Domains → Manage DNS Records.
4. Add a **CNAME** named **preview**, with the exact target displayed by the new Vercel domain card. The Vercel domain-configuration API currently recommends **cname.vercel-dns.com.** for this unassigned hostname. This was a read-only recommendation, not a provisioned project-domain record; if the domain card supplies a project-specific target, use that value instead. Use default TTL.
5. Add an ownership TXT record only if Vercel requests it, using the exact displayed name/value. No TXT challenge has been provided yet.
6. Preserve apex, www, nameservers, MX, SPF, DKIM, DMARC, and unrelated records. No Cloudflare zone/nameserver migration is needed for the observed Wix-hosted DNS.
7. Wait for valid configuration and SSL; verify this hostname resolves to the redesign Preview and follows the next reviewed integration merge.
8. Check logged-out access before sending the custom URL to clients. Current protection excludes custom domains; Joe must choose whether that public review behavior is intended or whether to protect it.

[Assign a domain to a branch](https://vercel.com/docs/domains/working-with-domains/assign-domain-to-a-git-branch) · [Use the domain's displayed DNS record](https://vercel.com/docs/domains/working-with-domains/add-a-domain)

## Hosted acceptance and future changes

- Verify each route directly and through React navigation, including Home and the event alias. RSC: 1 requests with a query string must return text/x-component data, not HTML.
- Explicit Vercel routes must run before filesystem lookup; high-level rewrites are too late when the matching HTML file exists. Keep RSC cache and indexing headers on those routes. [Vercel routing precedence](https://vercel.com/docs/project-configuration/vercel-json)
- Check noindex, no preview canonicals, empty sitemap, robots, assets, legacy PDF redirects, and the custom HTTP 404.
- Review mobile/tablet/desktop interactions and forms before client approval. HTTP checks do not prove visual quality or provider delivery.
- Merge reviewed feature PRs into redesign-2026. Share the stable branch alias until the custom staging domain is configured; do not send an unconfigured hostname.

For rollback, use a revert PR into redesign-2026 and verify its Preview. Retain earlier deployments and Git history. Follow the [launch and rollback checklist](launch-checklist.md) for a separately authorized production launch. This task neither deploys nor promotes production.
